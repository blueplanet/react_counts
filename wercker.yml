box: ruby:2.2.2
services:
    - postgres

build:
    steps:
        - bundle-install
        - script:
            name: rspec
            code: bundle exec rspec

deploy:
  steps:
    - heroku-deploy:
        key: $HEROKU_KEY
        user: $HEROKU_USER
        app-name: $HEROKU_APP_NAME
